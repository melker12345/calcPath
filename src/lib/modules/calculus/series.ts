import type { ModuleContent } from "../types";

/**
 * series (placeholder during migration)
 * Content will be moved here from the original monolithic modules.ts
 */
export const seriesModule: ModuleContent = {} as ModuleContent; // TODO: move content
