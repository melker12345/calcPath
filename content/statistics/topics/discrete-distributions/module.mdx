---
title: Discrete Probability Distributions
---

# Discrete Probability Distributions

A discrete probability distribution assigns probabilities to a countable set of outcomes. The binomial and Poisson distributions model the most common real-world counting processes.

The foundation is the random variable: a function that assigns a number to each outcome. Expected value and variance summarize its center and spread.

Mastering discrete distributions is the first step toward understanding statistical inference — all confidence intervals and hypothesis tests are built on distributional assumptions.

## Random Variables and Expected Value

A random variable $X$ maps sample space to reals. PMF gives $P(X=x_i)$.

$E(X) = \sum x_i P(X=x_i)$. The long-run average.

Var and SD defined accordingly.

**ELI5**: Expected value is the gravity center of the distribution — die averages 3.5 over many rolls even if never lands on 3.5.

## Binomial Distribution

Models number of successes in $n$ independent trials with success prob $p$.

PMF, mean $np$, var $np(1-p)$.

**Common Mistakes**: Using normal approx without $np, n(1-p) \ge 5$ or 10 rule of thumb.

## Poisson Distribution

Models rare events in fixed interval. Parameter $\lambda$ = mean = variance.

Approximates binomial when $n$ large, $p$ small, $\lambda = np$ moderate.

## Other Discrete (Geometric, etc.)

(Full content ported from legacy src/lib/modules/statistics/discrete-distributions.ts and questions file.)
